// Particle types
part_type_destroy(partTypeDust);
part_type_destroy(partTypePulse);
part_type_destroy(partTypeAfterImage);

// Particle systems
part_system_destroy(partSystem);
part_system_destroy(partSystemLow);